export function productsReducer(state,action) {
    switch (action.type) {
        case "AddToCart":{
            state:{...state}
        }
            
            break;
    
        default:
            break;
    }
}
